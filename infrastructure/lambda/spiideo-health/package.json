{
  "name": "playhub-spiideo-health",
  "version": "1.0.0",
  "description": "Lambda: polls the internal Spiideo API for scene/camera health + API-accessibility canary",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --external:@aws-sdk/*",
    "package": "npm run build && cd dist && zip -r ../dist.zip index.js",
    "deploy": "npm run package && cd ../terraform && terraform apply -target=aws_lambda_function.spiideo_health",
    "dry-run": "esbuild index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --external:@aws-sdk/* && DRY_RUN=1 node -e \"require('./dist/index.js').handler().then(r=>console.log(r)).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.0.0"
  }
}
