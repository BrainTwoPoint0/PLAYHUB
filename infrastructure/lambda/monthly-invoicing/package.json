{
  "name": "playhub-monthly-invoicing",
  "version": "1.0.0",
  "description": "Lambda function to generate monthly invoices for all active venues",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "package": "npm run build && cd dist && zip -r ../dist.zip index.js",
    "deploy": "npm run package && cd ../terraform && terraform apply -target=aws_lambda_function.monthly_invoicing"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
