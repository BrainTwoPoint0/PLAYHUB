{
  "name": "playhub-sync-recordings",
  "version": "1.0.0",
  "description": "Lambda function to sync Spiideo recordings to S3",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@aws-sdk/*",
    "package": "npm run build && cd dist && zip -r ../dist.zip index.js",
    "deploy": "npm run package && cd ../terraform && terraform apply -target=aws_lambda_function.sync_recordings"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-storage": "^3.700.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.0.0"
  }
}
