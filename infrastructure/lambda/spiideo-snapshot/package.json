{
  "name": "playhub-spiideo-snapshot",
  "version": "1.0.0",
  "description": "Lambda: on-demand raw-panorama snapshot of a Spiideo scene (live session → ffmpeg frame → Supabase Storage)",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --external:@aws-sdk/*",
    "package": "npm run build && cd dist && zip -r ../dist.zip index.js",
    "deploy": "npm run package && cd ../../terraform && terraform apply -target=aws_lambda_function.spiideo_snapshot"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.0.0"
  }
}
