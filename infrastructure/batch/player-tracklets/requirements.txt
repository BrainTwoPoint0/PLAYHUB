# Pinned exactly, matching aim-track: numerical pipeline — an OpenCV minor
# bump can shift RANSAC output between :latest image rebuilds.
opencv-python-headless==4.10.0.84
numpy==2.1.3
scipy==1.14.1
boto3==1.35.76
